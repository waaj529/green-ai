
### Final Audit Findings (Pixel-Perfect Comparison)

| Page | Discrepancy Found | Fix Needed |
| --- | --- | --- |
| **Media & Press** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". Missing "MEDIA & PRESS" vertical title outline on the left. | Update `MediaPress.tsx` to use `figmaPlaceholder="Let's Talk Energy"` and add the vertical title SVG. |
| **Expertise** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". Active card border color is slightly different. | Update `Expertise.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Our Story** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". Milestone image alignment needs adjustment. | Update `OurStory.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Certifications** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `Accrediations.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Sustainability** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". Right-side text "We lead with purpose..." has incorrect line-height/alignment. | Update `EsgCommitments.tsx` to use `figmaPlaceholder="Let's Talk Energy"` and fix text styling. |
| **GRID-INTEL** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `GridIntel.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Project Portfolio** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `Portfolio.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Client Partners** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `ClientPartnerships.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Collaboration** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `CollaborationInnovation.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |
| **Tech Alliances** | Chatbar placeholder is "Type your words..." instead of "Let's Talk Energy". | Update `TechnologyInnovationAlliances.tsx` to use `figmaPlaceholder="Let's Talk Energy"`. |

**Summary:** The most common remaining issue is the "Let's Talk Energy" chatbar placeholder missing across several pages, and a few missing vertical titles.
